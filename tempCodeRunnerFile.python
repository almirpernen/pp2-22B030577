import pygame

pygame.init()

screen = pygame.display.set_mode((1200, 800))

while True:
    for event in pygame.event.get():
        if event == pygame.QUIT:
            exit()

screen.fill((0 , 0, 0))